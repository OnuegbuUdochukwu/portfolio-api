[project]
name = "portfolio-api"
version = "0.1.0"
description = "Backend API for Udochukwu Onuegbu's portfolio"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"
